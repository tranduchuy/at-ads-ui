const prefix = '__ata__';

const CookieNames = {
  token: prefix + '_token',
  user: prefix + '_user',
  standBy: prefix + '_stand_by',
  activeAccountId: prefix + '_active_account_id',
  activeAdsAccountId:  prefix + '_active_ads_account_id',
  accessToken: prefix + '_accessToken',
  refreshToken: prefix + '_refreshToken',
};

export default CookieNames;
