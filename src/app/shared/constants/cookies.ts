const prefix = '__ata__';

const CookieNames = {
  token: prefix + '_token',
  user: prefix + '_user',
  activeAccountId: prefix + '_active_account_id',
  activeAdsAccountId:  prefix + '_active_ads_account_id'
};

export default CookieNames;
